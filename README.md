# fileInfo
## Build
```
$ go build .
```
## Run
```
# Show the help menu
./fileInfo -h

# Show info of '/tmp/myfile'
./fileInfo /tmp/myfile

# Show info of '/tmp/myfile' with verbose mode enabled
./fileInfo -v
```